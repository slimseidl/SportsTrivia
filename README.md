This program contains a basic set of sports related trivia questions coded usign C++. In it's current state, the program is static, meaning that every time it is ran, the program will ask the same questions. In the future, I plan to create a trivia game using an engine. It will also pull a random set of questions from a data source using an API so that the question may be different each time.
